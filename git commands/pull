###############
git pull
###############
git pull [options] [<repository> [<refspec>...]]
used to fetch and integrate the changes which are present in the remote repository to the local repository.
used to update the local branch with the changes available in the remote repository branch

git pull = git fetch + git merge
Fetch: Download the latest changes from the remote repository.
Merge: Integrate those changes into your current branch.

Basic Pull:
# git pull 
    - This will fetch changes from the remote repository you cloned from (usually origin) and merge them into your current branch.
# git pull origin main
    - This will fetch and merge changes from the main branch of the origin remote repository into your current branch.

Ensure that 
1. you are on the correct branch - git checkout <branch_name>
2. Pull the latest changes - git pull 
